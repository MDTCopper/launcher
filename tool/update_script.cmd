@echo off
rem ============================================================================
rem Copper Launcher 更新引导脚本（引导钩子）
rem
rem 这个文件随每个版本一起下发，放在启动器 exe 旁边；更新时由启动器在
rem 「新文件已覆盖到程序目录」之后、重启启动器之前调用，用来处理那些
rem 光靠覆盖不行的破坏性变更（数据布局改了、配置格式变了等）。
rem
rem 调用约定：
rem   %1 = 目标版本（release tag 形态，如 v0.0.1-alpha6），一定给
rem   %2 = 来源版本（同形态，如 v0.0.1-alpha5），拿不到时为空
rem   工作目录 = 程序目录（数据根通常也在这里，config.bin / versions 等）
rem
rem 写脚本时的几条硬要求：
rem   - 只用 exit /b 退出，不要用 exit：调用方是另一个 cmd 脚本，exit 会把
rem     它也一起结束掉（更新流程剩下的收尾就没了）
rem   - 迁移数据只用覆盖 / 改名，不要删除用户文件；解压阶段同样只覆盖同名文件
rem   - 不要假设自己一定被调用：用户也可能手动解压覆盖，那时这个脚本不跑，
rem     所以涉及数据迁移的逻辑要能重复执行（再跑一次不出事）
rem   - 内容保持可读即可，默认什么都不做：没有破坏性变更就原样 exit /b 0
rem
rem 例子（按需取用，这里只是写法示范）：
rem   if "%2"=="" goto done
rem   if exist "config.bin" copy /y "config.bin" "config.bin.bak" >nul
rem   :done
rem ============================================================================

exit /b 0
